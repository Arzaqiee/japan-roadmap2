Taruh app icon (1024x1024 PNG) dan splash image di sini sebelum `npx cap add android`,
lalu gunakan tool seperti `@capacitor/assets` untuk generate semua ukuran icon Android otomatis:

  npm install @capacitor/assets --save-dev
  npx capacitor-assets generate --android
